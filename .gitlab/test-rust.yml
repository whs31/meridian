astra rust test:
  stage: test-rust
  tags: [alse]
  allow_failure: true
  needs:
    - job: astra rust build
      artifacts: true

  cache:
    paths:
      - Cargo.lock
      - target/

  script:
    - cargo --version
    - cargo test